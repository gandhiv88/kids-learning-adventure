# Bug Log

| Bug ID | Description | Steps to reproduce | Expected result | Actual result | Severity | Root cause | Fix | Test coverage | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 3C-001 | Time questions crossing 12 o'clock could generate the wrong 12-hour answer. | Generate core time questions until `It is 12:00. What time is 30 minutes later?` appears. | Correct answer is `12:30`. | Correct answer was encoded as `1:30`. | High | Time arithmetic treated 12 as hour 12 before modulo conversion, so adding minutes inside the 12 o'clock hour wrapped to 1 too early. | Added 12-hour clock arithmetic that converts to zero-based clock minutes, applies the delta, then converts `0` back to `12`. | `tests/real-world-math.test.ts` checks the reproduced 12:00 + 30 minutes case. | Complete |
| 3C-002 | Visual-selection validation accepted a `correctOptionId` whose value did not match `correctAnswer`. | Validate a visual-selection question where `correctAnswer` is `8` but `correctOptionId` points to an option with value `4`. | Validation rejects the malformed question. | Validation returned `true`. | High | `validateQuestion` only checked that the id existed and that there were at least three visual options. | Validation now requires unique option ids, exactly one option matching `correctAnswer`, and `correctOptionId` pointing to that option. | `tests/interaction-regressions.test.ts` includes the malformed visual-selection fixture. | Complete |
| 3C-003 | After a wrong multiple-choice answer, the same wrong answer remained selected and the Check button stayed enabled. | Start Addition Trail, reach a multiple-choice question, choose an incorrect option, and press Check. | Incorrect option is marked tried, no answer remains selected, and Check is disabled until a different answer is chosen. | The wrong value remained in answer state, so Check could be pressed repeatedly without changing the answer. | Medium | Multiple-choice retry state tracked attempted choices but did not clear the selected answer after an incorrect submission. | Clear the selected entry after an incorrect multiple-choice submission while preserving the attempted-choice marker. | `tests/e2e/learning-adventure.spec.ts` verifies Check is disabled after an incorrect multiple-choice attempt until the correct option is selected. | Complete |
| 3C-004 | Welcome and completion copy still described the grouped adventure as only Number Forest. | Load the welcome screen after Milestone 3B. | Entry copy refers to the full Math Adventure. | Primary action said `Enter Number Forest`, and result copy said `FOREST ADVENTURE COMPLETE`. | Low | Milestone 3B changed lesson organization, but top-level copy was not updated. | Updated loading, welcome, entry button, and result copy to use Math Adventure / Adventure wording. | `tests/e2e/learning-adventure.spec.ts` uses `Enter Math Adventure` in browser flows. | Complete |
